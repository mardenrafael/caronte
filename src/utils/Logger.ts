// Avalira ideia de futaramento virar um framework | com suporte ao express como middleware e um logger completo
export default class Logger {
  public log(message: string): void {
    console.log(message);
  }
}
